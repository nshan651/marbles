[package]
name = "marbles"
version = "0.1.0"
edition = "2021"

[lib]
name = "marbles"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
